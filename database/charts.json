{
  "_default": {
    "1": {
      "id": "bar_customers",
      "name": "Bar - Customers by Region",
      "sql": "SELECT region, COUNT(customer_id) AS total_customers FROM customer GROUP BY region",
      "type": "bar",
      "xField": "region",
      "yField": "total_customers",
      "color": "#3b82f6"
    },
    "2": {
      "id": "line_customers",
      "name": "Line - Customers by Region",
      "sql": "SELECT region, COUNT(customer_id) AS total_customers FROM customer GROUP BY region",
      "type": "line",
      "xField": "region",
      "yField": "total_customers",
      "color": "#ef4444"
    },
    "3": {
      "id": "area_customers",
      "name": "Area - Customers by Region",
      "sql": "SELECT region, COUNT(customer_id) AS total_customers FROM customer GROUP BY region",
      "type": "area",
      "xField": "region",
      "yField": "total_customers",
      "color": "#22c55e"
    },
    "4": {
      "id": "column_line_customers",
      "name": "Column + Line - Customers by Region",
      "sql": "SELECT region, COUNT(customer_id) AS total_customers FROM customer GROUP BY region",
      "type": "column-line",
      "xField": "region",
      "yField": "total_customers",
      "color": "#a855f7"
    },
    "5": {
      "id": "heatmap_customers",
      "name": "Heatmap - Customers by Region",
      "sql": "SELECT region, COUNT(customer_id) AS total_customers FROM customer GROUP BY region",
      "type": "heatmap",
      "xField": "region",
      "yField": "total_customers"
    },
    "6": {
      "id": "pie_customers",
      "name": "Pie - Customers by Region",
      "sql": "SELECT region, COUNT(customer_id) AS total_customers FROM customer GROUP BY region",
      "type": "pie",
      "xField": "region",
      "yField": "total_customers",
      "colors": ["#22c55e", "#16a34a", "#15803d", "#166534"]
    }
  }
}
